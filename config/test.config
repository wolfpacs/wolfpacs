%% -*- mode: erlang -*-
%% Test configuration - suppress logging during tests

[{kernel, [
    %% Set logger level to emergency to suppress all normal logs during tests
    {logger_level, emergency},
    {logger, [
        %% Remove default console handler during tests
        {handler, default, undefined}
    ]}
]}].
